<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--include
        android:id="@+id/tab_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        layout="@layout/tab_other_mood"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" /-->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabMode="scrollable"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager"
        android:layout_width="@dimen/dimen_0dp"
        android:layout_height="@dimen/other_mood_height"
        app:layout_constraintTop_toBottomOf="@id/tab_header"
        app:layout_constraintStart_toStartOf="@id/tab_header"
        app:layout_constraintEnd_toEndOf="@id/tab_header" />

</androidx.constraintlayout.widget.ConstraintLayout>